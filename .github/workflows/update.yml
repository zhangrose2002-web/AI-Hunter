name: Auto Update AI Tools

on:
  schedule:
    - cron: '0 0,6,12,18 * * *'  # 每周日 UTC 00:00（北京时间 周日早8点）
  workflow_dispatch:     # 允许手动触发

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4 lxml

      - name: Run crawler
        run: python crawler_cn.py

      - name: Commit and push if changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json
          git commit -m "Auto update trends and tools" || exit 0
          git pull --rebase origin main
          git push origin main

      - name: 🚀 部署到我的空间
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}    # 你的空间 FTP 地址
          username: ${{ secrets.FTP_USERNAME }} # FTP 用户名
          password: ${{ secrets.FTP_PASSWORD }} # FTP 密码
          local-dir: ./                        # 上传当前目录所有文件
          server-dir: /wwwroot/ai-hunter/      # 你的空间目标文件夹
