name: Auto Update AI Tools

on:
  schedule:
    - cron: '0 0,6,12,18 * * *'  # 每周日 UTC 00:00（北京时间 周日早8点）
  workflow_dispatch:     # 允许手动触发

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4 lxml

      - name: Run crawler
        run: python crawler_cn.py

      - name: Commit and push if changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json
          # 只有在有变动时才 commit，防止空报错
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto update data" && git pull --rebase origin main && git push)
